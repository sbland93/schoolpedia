<header>{{school.name}}학생 프로필</header>
<div class="col-md-4">
	<ul class="profileList">
		{{#each profileList}}
			<a href="/profile/{{id}}">
				<li>
				<p>{{#each schools}}
					{{school.name}}
				{{/each}}</p>
				<p>이름:{{name}} 졸업년도: {{graduation}} {{school.name}}</p>
				</li>
			</a>
		{{/each}}
	</ul>

</div>
<!--프로필검색 form -->
<div class="col-md-8">
	<form class="form-horizontal searchBoardForm" id="searchBoardForm" role="form" action="api/profile/search" method="GET">
		<!-- DOLATER 학교내검색 변경  -->
		<input type="hidden" name="schoolId" value="{{schoolInfo.id}}">
		
		<div class="form-group">
			<label for="schoolDetail" class="col-md-3 control-label">학교</label>
			<input type="radio" name="school" value="only" id="schoolDetail" checked> {{schoolInfo.name}}내
			<input type="radio" name="school" value="all" id="schoolDetail"> 모든학교
		</div>

		<div class="form-group">
			<label for="fields" class="col-md-3 control-label">이름</label>
			<input type="radio" name="fields" value="only" id="fields" checked> 이름
			<input type="radio" name="fields" value="all" id="fields"> 본문포함(일화속 친구 포함)
		</div>

		<div class="form-group">
			<label for="searchField" class="col-md-3 control-label">검색내용</label>
			<div class="col-md-9">
				<input type="text" class="form-control" name="q" id="searchField" placeholder="검색...">
			</div>
		</div>

		<div class="form-group">
			<label for="graduation" class="col-md-3 control-label">졸업년도</label>
			<div class="col-md-6">
				<input type="text" class="form-control" name="graduation" id="graduation" placeholder="예) 1992, 1988">
			</div>
		</div>

		<div class="form-group">
			<label for="classNum" class="col-md-3 control-label">학급</label>
			<div class="col-md-6">
				<input type="text" class="form-control" name="classNum" id="classNum" placeholder="예) 107, 212, 314">
			</div>
		</div>

		<div class="form-group">
			<div class="col-sm-offset-9 col-md-offset-9">
				<button type="submit" class="btn btn-default buttonGroup">검색</button>
				<a href="/school/{{schoolInfo.id}}/profile/newTwo" class="btn btn-default buttonGroup">프로필생성</a>
			</div>
		</div>

	</form>
</div>

