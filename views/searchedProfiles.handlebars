<h1>Searched Profiles Page, This page include profile and sorting TODO</h1>

{{#if empty}}
	그런학생 없습니다.	
{{/if}}

<ul class="profileList">
	{{#each profileList}}
		<a href="/profile/{{id}}">
			<li>
			<p>이름: {{name}}</p>
			<p>나이(출생년도): {{age}}</p>
			{{#each schools}}
				{{school.name}}
			{{/each}}
			{{#each stories}}
				<p>{{content}}</p>
			{{/each}}
			<p class="updated_at">updated_at: {{updated_at}}</p>
			</li>
		</a>
	{{/each}}
</ul>

<a href="/school/{{schoolInfo.id}}/profile/new" class="btn btn-primary">글쓰기</a>


<form class="form-horizontal searchBoardForm" id="searchBoardForm" role="form" action="/profile/search" method="GET">

	<!-- DOLATER 학교내검색 변경  -->
	<input type="hidden" name="schoolId" value="{{schoolInfo.id}}">
	
	<div class="form-group">
		<label for="schoolDetail" class="col-sm-1 control-label">학교</label>
		<input type="radio" name="school" value="only" id="schoolDetail" checked> {{schoolInfo.name}}내에서
		<input type="radio" name="school" value="all" id="schoolDetail"> 모든학교에서
	</div>

	<div class="form-group">
		<label for="fields" class="col-sm-1 control-label">이름</label>
		<input type="radio" name="fields" value="only" id="fields" checked> 이름
		<input type="radio" name="fields" value="all" id="fields"> 본문포함(일화속 친구 포함)
	</div>

	<div class="form-group">
		<label for="searchField" class="col-sm-1 control-label">검색내용</label>
		<div class="col-sm-4">
			<input type="text" class="form-control" name="q" id="searchField" placeholder="검색...">
		</div>
	</div>

	<div class="form-group">
		<label for="age" class="col-sm-1 control-label">출생년도</label>
		<div class="col-sm-4">
			<input type="text" class="form-control" name="age" id="age" placeholder="예) 1992, 1988">
		</div>
	</div>

	<div class="form-group">
		<label for="classNum" class="col-sm-1 control-label">학급</label>
		<div class="col-sm-4">
			<input type="text" class="form-control" name="classNum" id="classNum" placeholder="예) 107, 212, 314">
		</div>
	</div>

	<div class="form-group">
		<div class="col-sm-offset-2 col-sm-4">
			<button type="submit" class="btn btn-default">검색</button>
		</div>
	</div>

</form>