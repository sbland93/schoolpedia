<header class="bugName">{{profile.bugName}}</header>

<div class="row">
	<div class="col-md-offset-10">
		
			<span>충호</span>: <em class="bugName">{{profile.bugName}}</em> <span>이름</span>: <em>{{profile.name}}</em>
	</div>
</div>
<div class="row">
	<div class="col-md-offset-8">
		{{#each profile.schools}}
			<span>학교</span>: <a class="updateClass" schoolId={{school._id}}><em>{{school.name}}</em></a>
			<span>학급</span>:
			{{#each class}}
				 <em>{{this}}반</em>
			{{/each}}
			</br>
		{{/each}}
	</div>
</div>